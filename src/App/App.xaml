<Application x:Class="StepsRecorder.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <tb:TaskbarIcon x:Key="TrayIcon"
                    IconSource="/Assets/tray-icon.ico"
                    ToolTipText="Steps Recorder — Idle">
      <tb:TaskbarIcon.ContextMenu>
        <ContextMenu>
          <MenuItem Header="Start Recording  (F9)" Name="MenuStart"   Click="MenuStart_Click"/>
          <MenuItem Header="Stop Recording   (F9)" Name="MenuStop"    Click="MenuStop_Click"  IsEnabled="False"/>
          <Separator/>
          <MenuItem Header="Pause Steps      (F10)" Name="MenuPause"  Click="MenuPause_Click" IsEnabled="False"/>
          <Separator/>
          <MenuItem Header="Open Output Folder"     Name="MenuFolder" Click="MenuFolder_Click"/>
          <Separator/>
          <MenuItem Header="Exit"                                      Click="MenuExit_Click"/>
        </ContextMenu>
      </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>
  </Application.Resources>
</Application>
