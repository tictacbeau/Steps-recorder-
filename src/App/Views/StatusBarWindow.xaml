<Window x:Class="StepsRecorder.App.Views.StatusBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:StepsRecorder.App.ViewModels"
        Title="StepsRecorder Status"
        Width="220" Height="44"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="20" Top="20">

  <Window.DataContext>
    <vm:StatusBarViewModel/>
  </Window.DataContext>

  <!-- Draggable pill -->
  <Border Background="#CC1E1E1E"
          CornerRadius="22"
          Padding="14,6"
          MouseLeftButtonDown="Border_MouseLeftButtonDown"
          Cursor="SizeAll">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
      <TextBlock Text="{Binding StateLabel}"
                 Foreground="White"
                 FontSize="12"
                 FontFamily="Segoe UI"
                 VerticalAlignment="Center"/>
      <TextBlock Text="  "
                 Foreground="White"
                 FontSize="12"
                 VerticalAlignment="Center"/>
      <TextBlock Text="{Binding ElapsedLabel}"
                 Foreground="#AAD4FF"
                 FontSize="12"
                 FontFamily="Consolas"
                 VerticalAlignment="Center"/>
    </StackPanel>
  </Border>
</Window>
